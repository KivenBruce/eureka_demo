eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8061/eureka/

spring:
  application:
    name: config
  cloud:
    config:
      discovery:
        enabled: true
        service-id: springcloud-config
      profile: dev
server:
  port: 8082